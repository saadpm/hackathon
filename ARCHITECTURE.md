# 🏗️ SkillPilot AI - System Architecture

## 📁 Project Structure

```
hackathon/
├── 📱 frontend/                    # Frontend Application (HTML + JS)
│   ├── index.html                  # Login page
│   ├── dashboard.html              # Main dashboard (role-based)
│   ├── css/
│   │   └── styles.css              # Complete styling (responsive)
│   └── js/
│       ├── config.js               # API configuration
│       ├── auth.js                 # Authentication utilities
│       ├── api.js                  # API service layer
│       ├── login.js                # Login handler
│       └── dashboard.js            # Dashboard logic
│
├── 🔧 backend/                     # FastAPI Backend
│   ├── app.py                      # Main FastAPI application
│   ├── config.py                   # Environment configuration
│   ├── schemas.py                  # Pydantic validation schemas
│   │
│   ├── database/
│   │   └── connection.py           # SQLAlchemy database setup
│   │
│   ├── models/                     # SQLAlchemy ORM Models
│   │   ├── __init__.py
│   │   ├── user.py                 # Users + RBAC
│   │   ├── job.py                  # Job Titles & Descriptions
│   │   ├── skill.py                # Employee Skills
│   │   ├── quiz.py                 # Quiz Questions & Results
│   │   └── roadmap.py              # Learning Roadmaps
│   │
│   ├── middleware/                 # Auth Middleware
│   │   ├── __init__.py
│   │   └── auth.py                 # JWT + RBAC implementation
│   │
│   ├── routers/                    # API Endpoints
│   │   ├── __init__.py
│   │   ├── auth.py                 # Login & Registration
│   │   ├── job.py                  # Job Title & JD management
│   │   ├── assessment.py           # Skills & Gap Analysis
│   │   ├── quiz.py                 # Quiz Generation & Submission
│   │   ├── roadmap.py              # Roadmap Generation
│   │   └── reports.py              # Employee Reports
│   │
│   ├── services/                   # Business Logic
│   │   ├── __init__.py
│   │   ├── openai_service.py       # OpenAI GPT-4 integration
│   │   └── vector_service.py       # FAISS vector database
│   │
│   └── Dockerfile                  # Backend container
│
├── 🗄️ database/
│   └── init.sql                    # MySQL schema + seed data
│
├── 🐳 Docker Configuration
│   ├── docker-compose.yml          # 3-service orchestration
│   └── nginx.conf                  # Reverse proxy config
│
├── ⚙️ Configuration
│   ├── .env                        # Environment variables (your settings)
│   ├── env.example                 # Template
│   └── requirements.txt            # Python dependencies
│
├── 🚀 Scripts
│   └── run.sh                      # One-command startup
│
└── 📚 Documentation
    ├── README.md                   # Main documentation
    ├── INSTALLATION.md             # Installation guide
    ├── QUICKSTART.md               # Quick start guide
    ├── PROJECT_SUMMARY.md          # What was built
    └── ARCHITECTURE.md             # This file
```

---

## 🎨 System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (Nginx:80)                           │
│  ┌────────────┐  ┌────────────┐  ┌──────────────────────────┐  │
│  │ index.html │  │dashboard.  │  │     JavaScript           │  │
│  │  (Login)   │  │   html     │  │  ┌──────────────────┐   │  │
│  │            │  │            │  │  │ auth.js          │   │  │
│  │            │  │            │  │  │ api.js           │   │  │
│  │            │  │            │  │  │ dashboard.js     │   │  │
│  └────────────┘  └────────────┘  │  └──────────────────┘   │  │
│                                   │     styles.css           │  │
│                                   └──────────────────────────┘  │
└──────────────────────────┬──────────────────────────────────────┘
                           │ HTTP/REST API
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                  BACKEND (FastAPI:8000)                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    API Layer (app.py)                     │  │
│  │  ┌────────┐ ┌────────┐ ┌──────────┐ ┌────────┐ ┌──────┐ │  │
│  │  │ Auth   │ │  Job   │ │Assessment│ │ Quiz   │ │Report│ │  │
│  │  │Router  │ │ Router │ │  Router  │ │ Router │ │Router│ │  │
│  │  └───┬────┘ └───┬────┘ └────┬─────┘ └───┬────┘ └──┬───┘ │  │
│  └──────┼──────────┼───────────┼───────────┼──────────┼─────┘  │
│         │          │           │           │          │         │
│  ┌──────▼──────────▼───────────▼───────────▼──────────▼─────┐  │
│  │              Middleware (JWT + RBAC)                      │  │
│  │  ┌────────────────┐  ┌──────────────────────────────┐    │  │
│  │  │ Authentication │  │ Role-Based Access Control    │    │  │
│  │  │   (JWT)        │  │ (OD_MANAGER / EMPLOYEE)      │    │  │
│  │  └────────────────┘  └──────────────────────────────┘    │  │
│  └────────────────────────────────────────────────────────────┘  │
│         │                                           │             │
│  ┌──────▼─────────────────────────┐    ┌───────────▼──────────┐ │
│  │      Business Logic            │    │     AI Services      │ │
│  │  ┌──────────────────────────┐  │    │  ┌────────────────┐ │ │
│  │  │  SQLAlchemy Models       │  │    │  │ OpenAI Service │ │ │
│  │  │  - User                  │  │    │  │  - Quiz Gen    │ │ │
│  │  │  - JobTitle              │  │    │  │  - Gap Analysis│ │ │
│  │  │  - JobDescription        │  │    │  │  - Roadmap Gen │ │ │
│  │  │  - EmployeeSkill         │  │    │  └────────────────┘ │ │
│  │  │  - QuizQuestion          │  │    │  ┌────────────────┐ │ │
│  │  │  - AssessmentResult      │  │    │  │ Vector Service │ │ │
│  │  │  - LearningRoadmap       │  │    │  │  - FAISS Index │ │ │
│  │  └──────────────────────────┘  │    │  │  - Embeddings  │ │ │
│  └────────────┬───────────────────┘    │  └────────────────┘ │ │
│               │                        └──────────┬────────────┘ │
└───────────────┼───────────────────────────────────┼──────────────┘
                │                                   │
                ▼                                   ▼
┌─────────────────────────────┐    ┌────────────────────────────┐
│   MySQL Database (3306)     │    │  External AI APIs          │
│  ┌───────────────────────┐  │    │  ┌──────────────────────┐ │
│  │ Tables:               │  │    │  │  OpenAI GPT-4        │ │
│  │ - users               │  │    │  │  (Quiz, Analysis,    │ │
│  │ - job_titles          │  │    │  │   Roadmap, Career)   │ │
│  │ - job_descriptions    │  │    │  └──────────────────────┘ │
│  │ - employee_skills     │  │    │                            │
│  │ - quiz_questions      │  │    │  Vector Store (Local)      │
│  │ - assessment_results  │  │    │  ┌──────────────────────┐ │
│  │ - learning_roadmaps   │  │    │  │  FAISS Index         │ │
│  └───────────────────────┘  │    │  │  Skill Embeddings    │ │
└─────────────────────────────┘    │  └──────────────────────┘ │
                                   └────────────────────────────┘
```

---

## 🔄 Data Flow Diagrams

### 1. Employee Skill Assessment Flow

```
Employee                 Frontend              Backend                 MySQL
   │                        │                     │                      │
   │  1. Add Skills         │                     │                      │
   ├───────────────────────>│                     │                      │
   │                        │  POST /assessment/  │                      │
   │                        │   submit-skills     │                      │
   │                        ├────────────────────>│                      │
   │                        │                     │  Validate JWT        │
   │                        │                     │  Check Role=EMPLOYEE │
   │                        │                     │                      │
   │                        │                     │  Save Skills         │
   │                        │                     ├─────────────────────>│
   │                        │                     │                      │
   │                        │                     │  Create Embeddings   │
   │                        │                     ├──────────┐           │
   │                        │                     │  FAISS   │           │
   │                        │                     │<─────────┘           │
   │                        │                     │                      │
   │                        │  ✓ Skills Saved     │                      │
   │                        │<────────────────────┤                      │
   │  ✓ Success             │                     │                      │
   │<───────────────────────┤                     │                      │
```

### 2. AI-Powered Gap Analysis Flow

```
Employee              Frontend           Backend              MySQL         OpenAI
   │                     │                  │                   │             │
   │  View Gap Analysis  │                  │                   │             │
   ├────────────────────>│                  │                   │             │
   │                     │  GET /assessment/│                   │             │
   │                     │   gap-analysis   │                   │             │
   │                     ├─────────────────>│                   │             │
   │                     │                  │  Get User Skills  │             │
   │                     │                  ├──────────────────>│             │
   │                     │                  │                   │             │
   │                     │                  │  Get JD for Role  │             │
   │                     │                  ├──────────────────>│             │
   │                     │                  │                   │             │
   │                     │                  │  Vector Compare   │             │
   │                     │                  ├─────────┐         │             │
   │                     │                  │ FAISS   │         │             │
   │                     │                  │<────────┘         │             │
   │                     │                  │                   │             │
   │                     │                  │  AI Gap Analysis  │             │
   │                     │                  ├─────────────────────────────────>│
   │                     │                  │                   │  GPT-4      │
   │                     │                  │  Priorities, Time │  Analysis   │
   │                     │                  │<─────────────────────────────────┤
   │                     │                  │                   │             │
   │                     │  Gap Report      │                   │             │
   │                     │  (Vector + AI)   │                   │             │
   │                     │<─────────────────┤                   │             │
   │  View Results       │                  │                   │             │
   │<────────────────────┤                  │                   │             │
```

### 3. Quiz Generation Flow (OD Manager)

```
OD Manager           Frontend           Backend              MySQL         OpenAI
   │                     │                  │                   │             │
   │  Generate Quiz      │                  │                   │             │
   ├────────────────────>│                  │                   │             │
   │  (Job, Skill, Exp)  │                  │                   │             │
   │                     │  POST /quiz/     │                   │             │
   │                     │   generate       │                   │             │
   │                     ├─────────────────>│                   │             │
   │                     │                  │  Validate JWT     │             │
   │                     │                  │  Check OD_MANAGER │             │
   │                     │                  │                   │             │
   │                     │                  │  Get Job Title    │             │
   │                     │                  ├──────────────────>│             │
   │                     │                  │                   │             │
   │                     │                  │  Generate Quiz    │             │
   │                     │                  ├─────────────────────────────────>│
   │                     │                  │                   │  GPT-4      │
   │                     │                  │  Questions        │  Creates    │
   │                     │                  │  (MCQ + Answers)  │  Questions  │
   │                     │                  │<─────────────────────────────────┤
   │                     │                  │                   │             │
   │                     │                  │  Save Questions   │             │
   │                     │                  ├──────────────────>│             │
   │                     │                  │                   │             │
   │                     │  ✓ Quiz Ready    │                   │             │
   │                     │  (Preview)       │                   │             │
   │                     │<─────────────────┤                   │             │
   │  Review & Approve   │                  │                   │             │
   │<────────────────────┤                  │                   │             │
```

---

## 🔐 Security Architecture

### Authentication Flow

```
1. User Login
   ├─> Frontend sends credentials
   ├─> Backend validates against database
   ├─> Password verified using bcrypt
   ├─> JWT token generated
   └─> Token sent to frontend

2. Authenticated Requests
   ├─> Frontend includes JWT in Authorization header
   ├─> Middleware validates token
   ├─> Decodes user ID and role
   ├─> Checks role permissions
   └─> Allows or denies access
```

### Role-Based Access Control (RBAC)

```
┌──────────────────────────────────────────────┐
│            API Endpoint                      │
└──────────────┬───────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────┐
│      Authentication Middleware               │
│  ┌────────────────────────────────────────┐  │
│  │  1. Extract JWT from header            │  │
│  │  2. Verify token signature             │  │
│  │  3. Check expiration                   │  │
│  │  4. Extract user ID and role           │  │
│  └────────────────────────────────────────┘  │
└──────────────┬───────────────────────────────┘
               │
               ▼
┌──────────────────────────────────────────────┐
│      Authorization Check                     │
│  ┌────────────────────────────────────────┐  │
│  │  Role Requirements:                    │  │
│  │  ├─ OD_MANAGER only                    │  │
│  │  │  - Generate quiz                    │  │
│  │  │  - View all employees               │  │
│  │  │  - Manage JDs                       │  │
│  │  │                                      │  │
│  │  └─ EMPLOYEE only                      │  │
│  │     - Submit skills                    │  │
│  │     - Take quiz                        │  │
│  │     - View roadmap                     │  │
│  └────────────────────────────────────────┘  │
└──────────────┬───────────────────────────────┘
               │
               ▼
        ✓ Access Granted
        ✗ 403 Forbidden
```

---

## 🗄️ Database Schema

### Entity Relationship Diagram

```
┌─────────────────┐           ┌──────────────────┐
│     users       │           │   job_titles     │
├─────────────────┤           ├──────────────────┤
│ id (PK)         │◄──────────│ id (PK)          │
│ name            │           │ title            │
│ email           │           │ level            │
│ password_hash   │           │ next_level_id(FK)│──┐
│ role (ENUM)     │           │ department       │  │
│ job_title_id(FK)├──────────►└──────────────────┘  │
│ years_exp       │                    ▲             │
└────────┬────────┘                    │             │
         │                             │             │
         │                   ┌─────────┴────────┐    │
         │                   │ job_descriptions │◄───┘
         │                   ├──────────────────┤
         │                   │ id (PK)          │
         │                   │ job_title_id(FK) │
         │                   │ required_skills  │
         │                   │ required_tools   │
         │                   │ required_exp     │
         │                   └──────────────────┘
         │
         │
         ├───────────────────────┐
         │                       │
         ▼                       ▼
┌─────────────────┐     ┌──────────────────┐
│ employee_skills │     │ assessment_      │
├─────────────────┤     │   results        │
│ id (PK)         │     ├──────────────────┤
│ user_id (FK)    │     │ id (PK)          │
│ skill_name      │     │ user_id (FK)     │
│ proficiency     │     │ quiz_question_id │
│ years_exp       │     │ selected_answer  │
│ score           │     │ is_correct       │
└─────────────────┘     │ points_earned    │
                        └────────┬─────────┘
                                 │
         ┌───────────────────────┘
         │
         ▼
┌─────────────────┐
│ quiz_questions  │
├─────────────────┤
│ id (PK)         │
│ job_title_id(FK)│
│ skill_name      │
│ question_text   │
│ options (JSON)  │
│ correct_answer  │
│ difficulty      │
│ created_by (FK) │
└─────────────────┘

┌─────────────────┐
│ learning_       │
│   roadmaps      │
├─────────────────┤
│ id (PK)         │
│ user_id (FK)    │
│ job_title_id(FK)│
│ skill_name      │
│ current_level   │
│ target_level    │
│ milestones(JSON)│
│ courses (JSON)  │
│ status          │
│ progress %      │
└─────────────────┘
```

---

## 🧠 AI Services Architecture

### OpenAI Service

```
┌────────────────────────────────────────────┐
│         OpenAI Service                     │
├────────────────────────────────────────────┤
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │  generate_quiz_questions()           │ │
│  │  Input:                              │ │
│  │    - skill_name                      │ │
│  │    - job_title                       │ │
│  │    - experience_level_years          │ │
│  │    - difficulty_level                │ │
│  │  Output: List[Question]              │ │
│  └──────────────────────────────────────┘ │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │  analyze_skill_gap()                 │ │
│  │  Input:                              │ │
│  │    - employee_skills                 │ │
│  │    - required_skills                 │ │
│  │    - job_title                       │ │
│  │    - required_experience_years       │ │
│  │  Output: GapAnalysis                 │ │
│  └──────────────────────────────────────┘ │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │  generate_learning_roadmap()         │ │
│  │  Input:                              │ │
│  │    - skill_name                      │ │
│  │    - current_level                   │ │
│  │    - target_level                    │ │
│  │    - gap_percentage                  │ │
│  │  Output: Roadmap                     │ │
│  └──────────────────────────────────────┘ │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │  suggest_career_progression()        │ │
│  │  Input:                              │ │
│  │    - current_job_title               │ │
│  │    - current_skills                  │ │
│  │    - years_of_experience             │ │
│  │    - available_next_roles            │ │
│  │  Output: CareerSuggestion            │ │
│  └──────────────────────────────────────┘ │
└────────────────────────────────────────────┘
```

### Vector Service (FAISS)

```
┌────────────────────────────────────────────┐
│         Vector Service (FAISS)             │
├────────────────────────────────────────────┤
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │  Sentence Transformer                │ │
│  │  (all-MiniLM-L6-v2)                  │ │
│  │  Dimension: 384                      │ │
│  └──────────────────────────────────────┘ │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │  create_skill_embedding()            │ │
│  │  Input: skill_text + proficiency     │ │
│  │  Output: 384-dim vector              │ │
│  └──────────────────────────────────────┘ │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │  compare_skill_sets()                │ │
│  │  Input:                              │ │
│  │    - employee_skills                 │ │
│  │    - required_skills                 │ │
│  │  Process:                            │ │
│  │    1. Create embeddings for both     │ │
│  │    2. Calculate cosine similarity    │ │
│  │    3. Find best matches              │ │
│  │  Output:                             │ │
│  │    - missing_skills                  │ │
│  │    - matched_skills                  │ │
│  │    - similarity_scores               │ │
│  │    - gap_percentage                  │ │
│  └──────────────────────────────────────┘ │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │  FAISS Index                         │ │
│  │  Type: IndexFlatL2                   │ │
│  │  Persistent: vector_store/           │ │
│  └──────────────────────────────────────┘ │
└────────────────────────────────────────────┘
```

---

## 🔄 API Flow Example

### Complete Flow: Employee Takes Quiz

```
1. Employee Dashboard
   GET /quiz/questions?job_title_id=1
   └─> Returns list of questions (without correct answers)

2. Employee Answers Questions
   Frontend stores answers locally

3. Submit Quiz
   POST /quiz/submit
   Body: {
     answers: [
       {quiz_question_id: 1, selected_answer: "B", time_taken_seconds: 45},
       {quiz_question_id: 2, selected_answer: "A", time_taken_seconds: 30}
     ]
   }
   
4. Backend Processing
   ├─> Validate JWT (Employee role)
   ├─> For each answer:
   │   ├─> Get question from DB
   │   ├─> Compare answer with correct_answer
   │   ├─> Calculate points
   │   └─> Save to assessment_results
   ├─> Calculate total score
   └─> Return summary

5. Response
   {
     message: "Quiz submitted successfully",
     total_questions: 10,
     correct_answers: 7,
     total_score: 70,
     percentage: 70.0
   }

6. Update Roadmap (Optional)
   Based on quiz results, suggest updates to learning path
```

---

## 🚀 Deployment Architecture

### Docker Compose Services

```
┌─────────────────────────────────────────────────────────┐
│                    Docker Network                        │
│                  (skillpilot_network)                    │
│                                                          │
│  ┌────────────────┐  ┌────────────────┐  ┌───────────┐ │
│  │   MySQL        │  │   Backend      │  │ Frontend  │ │
│  │   Container    │  │   Container    │  │ Container │ │
│  ├────────────────┤  ├────────────────┤  ├───────────┤ │
│  │ Port: 3306     │  │ Port: 8000     │  │ Port: 80  │ │
│  │ Image: mysql:8 │  │ Image: Python  │  │Image:nginx│ │
│  │ Volume:        │  │ Volume:        │  │ Volume:   │ │
│  │  mysql_data    │  │  vector_store  │  │ frontend/ │ │
│  └────────────────┘  └────────────────┘  └───────────┘ │
│         ▲                    ▲                  ▲       │
└─────────┼────────────────────┼──────────────────┼───────┘
          │                    │                  │
          │                    │                  │
      Host:3306            Host:8000          Host:80
```

---

## 📊 Technology Stack

### Backend
- **Framework**: FastAPI 0.104.1
- **ORM**: SQLAlchemy 2.0.23
- **Database Driver**: PyMySQL 1.1.0
- **Authentication**: python-jose + passlib
- **AI**: OpenAI 1.3.7
- **Vector DB**: faiss-cpu 1.7.4
- **Embeddings**: sentence-transformers 2.2.2
- **Server**: Uvicorn 0.24.0

### Frontend
- **HTML5** + **CSS3**
- **Vanilla JavaScript** (ES6+)
- **No frameworks** - lightweight and fast

### Database
- **MySQL 8.0**
- **7 tables** with proper relationships
- **Indexes** for performance

### Infrastructure
- **Docker** + **Docker Compose**
- **Nginx** for frontend
- **Persistent volumes** for data

---

## 🎯 Design Patterns Used

1. **Repository Pattern**: Database access through ORM
2. **Service Layer Pattern**: Business logic in services/
3. **Dependency Injection**: FastAPI's Depends()
4. **Middleware Pattern**: Authentication layer
5. **Strategy Pattern**: Multiple AI service methods
6. **Singleton Pattern**: Service instances
7. **Factory Pattern**: JWT token creation

---

This architecture provides a scalable, maintainable, and secure foundation for the SkillPilot AI platform! 🚀

